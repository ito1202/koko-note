# Fault Table

This directory stores recurrence-prevention records for development failures.

## Files
- `fault-table.md`: master table for incidents, root causes, and prevention rules.

## Update Policy
- Add a new row when a new failure pattern is found.
- Update `prevention_rule` with actionable and testable behavior.
- Keep entries short and concrete.

